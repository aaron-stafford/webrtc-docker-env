Dockerfile and scripts for building the x86_64 version of the official Unity
Webrtc plugin.

Requires approximately 80gig disk space.

./install-docker.sh # (Optional) To install docker into a fresh environment (ubuntu) run the following
./run.sh # To execute the build environment

Artifacts are available in the /result folder.
